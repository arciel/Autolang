sudo: required

language: cpp

os: linux

compiler: gcc

sudo: false

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-5
      - gcc-5

install: export CXX="g++-5" CC="gcc-5"

script: make